# Firebase configuration

Place the real Firebase configuration file at:

```text
app/google-services.json
```

Do not commit `google-services.json`; it is ignored by git because it contains project credentials.
